# 盛意通经营平台 Demo 本地预览说明

## 推荐方式：直接预览已构建版本

1. 解压压缩包。
2. 双击 `启动本地预览.bat`。
3. 浏览器打开：

```text
http://127.0.0.1:4173/
```

如果端口被占用，可以手动执行：

```powershell
python -m http.server 4180 -d dist
```

然后访问：

```text
http://127.0.0.1:4180/
```

## 开发方式

如果需要继续修改源码，请先安装 Node.js，然后在项目根目录执行：

```powershell
npm install
npm run dev
```

## 说明

- `dist` 是已经构建好的可预览版本。
- `src` 是 React + TypeScript 源码。
- `public` 是图片、logo 等静态资源。
- 压缩包不包含 `node_modules`，避免文件过大。
- 不建议直接双击 `dist/index.html`，前端资源路径可能无法正常加载，请使用本地服务访问。
